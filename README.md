Game Design Methodologies.Third attempt at Git after breaking some files.

Pac-Man Clone.

Following Git workflow as recommended by tutor.

Work through different Feature-branches.

Commands:

git checkout ""

git add -A

git commit -m ""

git push origin ""

git checkout development

git pull origin development

git merge X

git push origin X

git workflow:

create git repository
create development from master
create feature branches from development
start on first branch
when done, pull origin development then merge into development
next branch, pull origin development and merge wip branch first before starting work.
